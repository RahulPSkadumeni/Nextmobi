<section>
    <div class="container">
        <div class="container">
  <div class="row">
    <div></div><div>
         <a type="button" href="/admin/add-category" class="btn btn-success ml-5">Add Category</a>

    </div>
    
    <div class="col-12">
      <table class="table table-bordered">
        <thead>
          <tr>
            <th style="in" scope="col">No</th>
            <th scope="col">Title</th>
             <th scope="col">Offer</th>
          
               <th scope="col">action</th>
          
           
          </tr>
        </thead>
       
           
        <tbody>
            <div class="container">
                
            </div>
            
             {{#each category}}
          <tr>
            <td scope="row"></td>
            <td>{{this.category}}</td>
            <td>{{this.offer}} %</td>
           
            
              {{!-- <td>{{this.category}}</td> --}}
              
             


            <td>
          
              {{!-- <button type="button" href="/admin/edit-product/{{this._id}}" class="btn btn-primary"><i class="fas fa-edit">Edit</i></button> --}}
               {{!-- <a  type="button" href="/admin/edit-product/{{this._id}}" class="btn btn-primary"  ><button onclick="return confirm(are you sure)">Edit</button></a> --}}
             <a  type="button" href="/admin/delete-category/{{this._id}}" class="btn btn-danger ml-5" onclick="return confirm('Are you want to delete {{this.category}} ?')"><button onclick="return confirm(are you sure)">delete</button></a>
            </td>
            
          </tr>
           {{/each}}
        </tbody>
      </table>

      <form action="/admin/add-offer" method="POST"  class="form-control validate" required>
                  {{!-- <div class="form-group mb-">
                    <label for="name">Category
                    </label>
                    <input id="category" name="category" type="text" class="form-control validate" required="">
                  </div>

                   <div class="form-group mb-">
                    <label for="name">Offer
                    </label>
                    <input id="category" name="offer" type="number" class="form-control validate" required="">
                  </div>
       
              <div class="col-lg-6 col-md-12 mt-5">
                <button type="submit" class="btn btn-primary ">Add offer</button> --}}

            <table class="table table-striped">
  <thead>
    <tr>
      <th scope="col">Category</th>
     
      <th scope="col">Offer %</th>
    </tr>
  </thead>
  <tbody>

    
    <tr>

      <th scope="row">
         
        <select name="category">
          {{#each category}}
          <option id="shipped"  value={{this._id}}>{{this.category}}</option>
           {{/each}}
          </select>
         
      </th>
     
      <td><input name="offer" type="number"></td>
    </tr>
    

  </tbody>
</table>


                 <button type="submit" class="btn btn-success ml-5" >add-offer</button>
              </div> 
            
              </form>
    </div>
  </div>
</div>
    </div>


</section>
<script>
    var table = document.getElementsByTagName('table')[0],
        rows = table.getElementsByTagName('tr'),
        text = 'textContent' in document ? 'textContent' : 'innerText';

    for (var i = 1, len = rows.length; i < len; i++) {
        rows[i].children[0][text] = i;
    }

</script>