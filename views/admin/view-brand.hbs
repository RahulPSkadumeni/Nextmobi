<section>
    <div class="container">
        <div class="container">
  <div class="row">
    <div></div><div>
         <a type="button" href="/admin/add-brand" class="btn btn-success ml-5">Add Brands</a>

    </div>
    
    <div class="col-12">
      <table class="table table-bordered">
        <thead>
          <tr>
            <th style="in" scope="col">No</th>
            <th scope="col">Title</th>
           
          </tr>
        </thead>
       
           
        <tbody>
            <div class="container">
                
            </div>
            
             {{#each brand}}
          <tr>
            <td scope="row"></td>
            <td>{{this.brand}}</td>
            
              {{!-- <td>{{this.category}}</td> --}}
              
             


            <td>
          
              {{!-- <button type="button" href="/admin/edit-product/{{this._id}}" class="btn btn-primary"><i class="fas fa-edit">Edit</i></button> --}}
               {{!-- <a  type="button" href="/admin/edit-product/{{this._id}}" class="btn btn-primary"  ><button onclick="return confirm(are you sure)">Edit</button></a> --}}
             <a  type="button" href="/admin/delete-brand/{{this._id}}" class="btn btn-danger" onclick="return confirm('Are you want to delete {{this.brand}} ?')"><button onclick="return confirm(are you sure)">delete</button></a>
            </td>
          </tr>
           {{/each}}
        </tbody>
      </table>
    </div>
  </div>
</div>
    </div>


</section>
<script>
    var table = document.getElementsByTagName('table')[0],
        rows = table.getElementsByTagName('tr'),
        text = 'textContent' in document ? 'textContent' : 'innerText';

    for (var i = 1, len = rows.length; i < len; i++) {
        rows[i].children[0][text] = i;
    }

</script>